language: ja-JP

reviews:
  profile: "assertive"
  request_changes_workflow: false
  high_level_summary: true
  high_level_summary_placeholder: "@coderabbitai summary"
  poem: false
  review_status: true
  collapse_walkthrough: false
  auto_review:
    enabled: true
    drafts: false
    ignore_title_keywords:
      - "WIP"
  finishing_touches:
    docstrings:
      enabled: true
    unit_tests:
      enabled: true
  instructions: |
    このプロジェクトのレビュー時は以下の観点を重視してください:
    ADRディレクトリにあるルールに抵触していないか必ず確認してください。

  pre_merge_checks:
    title:
      mode: error
      requirements: "rabbit-javaのprefixがついていること"
    custom_checks:
      - name: "テストコードの確認"
        mode: error
        instructions: "新規関数にはユニットテストが必ず存在すること"

knowledge_base:
  learnings:
    scope: local
  code_guidelines:
    filePatterns:
      - "**/ADR/*.md"
chat:
  auto_reply: true
